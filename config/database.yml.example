# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: postgresql
  database: kp-dev
  username: root
  password:
  encoding: utf8

# KP uses views so postgresql is needed for test as well.
test:
  adapter: postgresql
  database: kp-test
  username: root
  password:
  encoding: utf8

production:
  adapter: postgresql
  database: kp-prod
  username: root
  password:
  encoding: utf8
