<script type="text/javascript">
  kpConfig = {
    sessionfix: {
      url: "<%= url_for :controller => "login", :action => "session_fix" %>"
    },
    schools: {
      list: { url: "<%= options_list_schools_path %>" }
    },

    groups: {
      list: { url: "<%= options_list_groups_path %>" }
    },

    booking: {
      bookingInput: { url: "/booking/get_input_area" }
    },

    by_group: {
      byGroupList: {url: "/booking/get_by_group_list"}
    }
    
    <% if @occasion %>
    ,notreq: {
      notreqInput: { url: "<%= get_input_area_occasion_notification_requests_path(@occasion) %>" }
    }
    <% end %>

  };
</script>

