<h1>Kulturproceduren - Avbokning</h1>

<p>
  Biljetterna för
  <%= h @booking.group.name %>, <%= @booking.group.school.name %>
  till
  <%= h @booking.occasion.event.name %>,
  föreställningen
  <%= h @booking.occasion.date %> kl <%= l @booking.occasion.start_time, :format => :only_time %>
  har avbokats av
  <%= h @user.name %> (<%= h @user.email %>, <%= h @user.cellphone %>).
</p>

<% if @booking.bus_booking? %>
<p>
  Denna bokning innehåller en bussbokning:
</p>
<ul>
  <li><strong>Från hållplats:</strong> <%= h @booking.bus_stop %></li>
  <li><strong>Bokningstyp:</strong> <%= h @booking.bus_one_way ? "Enkel resa" : "Tur och retur" %></li>
</ul>
<% end %>

<% if @answer_form %>
  <p>
    <b>Enkätsvar:</b>
    <ul>
      <% @answer_form.answers.each do |answer| -%>
        <% question = answer.question -%>
        <li>
          <b><%= question.question %>:</b>
          <%=
          case question.qtype
          when "QuestionMark", "QuestionText" then h(answer.answer_text)
          when "QuestionBool" then answer.answer_text == "y" ? "Ja" : "Nej"
          when "QuestionMchoice"
            choices = YAML.load(answer.answer_text).collect { |c| "<li>#{h(c)}</li>" }
            "<ul>#{choices}</ul>"
          end
          -%>
        </li>
      <% end %>
    </ul>
  </p>
<% end %>

