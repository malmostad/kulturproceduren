<%= render partial: "menu" %>

<table>
  <thead>
    <tr>
      <th>Termin</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @terms.reverse.each do |term| %>
      <tr class="<%= cycle("", "even") %>">
        <td><%= term %></td>
        <td>
          <%= link_to "Besökstatistik", visitors_statistic_path(term) -%>
        </td>
        <td>
          <%= link_to "Enkätstatistik", questionnaires_statistic_path(term) -%>
        </td>
        <td>
          <%= link_to "Avbokningsstatistik", unbooking_questionnaires_statistic_path(term) -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
