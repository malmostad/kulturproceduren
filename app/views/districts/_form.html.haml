= simple_form_for district, html: { class: "basic" } do |f|
  = f.error_notification class: "warning"

  - if district.new_record?
    = f.input :school_type_id,
      as: :select,
      collection: school_types
  = f.input :name
  = f.input :contacts, hint: "Ange e-postadresser, separerade med kommatecken"

  .form-group
    %span.control-label
    .controls
      = submit_tag "Spara", class: "btn btn-primary"
      - if district.new_record?
        = link_to "Avbryt", districts_path, class: "btn btn-default"
      - else
        = link_to "Ta bort",
          @district,
          method: :delete,
          data: { confirm: "Är du säker?" },
          class: "btn btn-danger"
