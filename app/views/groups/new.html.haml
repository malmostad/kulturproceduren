- content_for(:page_title) do
  Lägg till ny grupp

%main.admin.with-aside
  = render partial: "form", locals: { group: @group, schools: @schools }

%aside.admin
  = render partial: "menu"
  = render partial: "shared/admin_menu"
